package com.pablo.paper.domain.model

enum class ReaderMode {
    READING,
    INK,
    MARKDOWN,
    ASSISTANT
}

enum class ViewMode {
    FULL_PAGE,
    FIT_WIDTH,
    ACTUAL_SIZE,
    CONTINUOUS_SCROLL,
    TWO_PAGE
}

enum class AppThemeMode {
    SYSTEM,
    LIGHT,
    DARK,
    SEPIA
}
